-- ActuatorConfig
-- WARNING: Do not commit real credentials to version control.
-- Replace values below before deployment.
-- Consider fetching from DataStore or a secure BindableFunction
-- resolver for production use.

local ActuatorConfig = {
	API_URL = "https://myurlhere.com/api",
	API_KEY = "REPLACE_WITH_KEY",
}

return ActuatorConfig